{
  "name": "backend",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev --local",
    "deploy": "wrangler deploy",
    "d1:migrate:local": "wrangler d1 execute hr_dashboard_db --local --file migrations/0001_init.sql",
    "d1:migrate:remote": "wrangler d1 execute hr_dashboard_db --remote --file migrations/0001_init.sql"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250101.0",
    "typescript": "^5.6.0",
    "wrangler": "^4.0.0"
  },
  "dependencies": {
    "zod": "^3.24.0"
  }
}