# HR Admin Emails (comma-separated)
HR_ADMIN_EMAILS=massimo@ticktockloans.com

# Database Path (defaults to ./data/hr_dashboard.db)
# DATABASE_PATH=./data/hr_dashboard.db

# Server Port (defaults to 3000)
# PORT=3000

# For local development, use query parameter ?as=email@example.com
# In production, set X-User-Email header or use Cloudflare Access
